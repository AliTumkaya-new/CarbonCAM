# Vercel/Linux build için optionalDependencies (örn. @parcel/watcher prebuild) kurulmalı
include=optional
optional=true

# Mevcut projede peer-deps çatışmalarını azaltmak için
legacy-peer-deps=true
